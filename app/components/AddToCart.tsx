"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("click")}>Click</button>
    </div>
  );
};

export default AddToCart;
